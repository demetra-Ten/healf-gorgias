# Internal Healf content — must NOT be published to the public repo.
# These are loaded straight into Supabase; they never need to live in git.
macros.sql
seed-*.sql
seed-*.json
# Customer Obsession: REAL customer data + internal Slack + a legal threat — never publish publicly.
customer-obsession.html
co-*.json
