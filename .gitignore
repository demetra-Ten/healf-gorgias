# Internal Healf content — must NOT be published to the public repo.
# These are loaded straight into Supabase; they never need to live in git.
macros.sql
seed-*.sql
seed-*.json
# Customer Obsession raw source data (un-anonymised) — never publish.
co-*.json
# customer-obsession.html IS published (customer PII anonymised + noindex), per explicit decision 2026-07-15.
# It names brands + describes internal incidents — keep the URL closely held; don't link it publicly.
